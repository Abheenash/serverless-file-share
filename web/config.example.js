// Copy to config.js and set your API base URL (no trailing slash).
// The deploy script (scripts/stage4_deploy.sh) generates config.js automatically.
window.SFS_CONFIG = {
  apiBase: "https://REPLACE_ME.execute-api.us-east-1.amazonaws.com",
};
